name: Release

on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

env:
  GO_VERSION: '1.26.5'

jobs:
  build:
    name: Build darwin/arm64
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Check source
        run: |
          go test ./...
          go vet ./...
          bash -n test-native-workload-lifecycle.sh

      - name: Build Darwin arm64 binary
        env:
          GOOS: darwin
          GOARCH: arm64
          CGO_ENABLED: '0'
        run: |
          mkdir -p dist/maclet-darwin-arm64
          go build -o dist/maclet-darwin-arm64/maclet .
          file dist/maclet-darwin-arm64/maclet

      - name: Package binary
        run: |
          tar -C dist -czf maclet-darwin-arm64.tar.gz maclet-darwin-arm64
          sha256sum maclet-darwin-arm64.tar.gz > maclet-darwin-arm64.tar.gz.sha256

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: maclet-darwin-arm64
          path: |
            maclet-darwin-arm64.tar.gz
            maclet-darwin-arm64.tar.gz.sha256
          if-no-files-found: error

  release:
    name: Create GitHub release
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: maclet-darwin-arm64
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: Build ${{ github.run_number }}
          generate_release_notes: true
          files: |
            dist/*.tar.gz
            dist/*.sha256
